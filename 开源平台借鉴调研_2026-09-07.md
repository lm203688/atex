# 开源平台借鉴调研（Realcast v0.7.7 → 互补/提升方向）

> 目标：扫描开源生态，找出对「真测 Realcast」（合规积分预测社区：类 Manifold × Good Judgment Open × Kalshi 信息呈现，FastAPI+SQLite，积分只送不卖/不可流通/不可回兑/去加密）有**互补或提升**价值的项目，给出可落地的借鉴研发路径。
> 调研日期：2026-09-07。结论基于 GitHub 仓库（stars/license/语言）+ 公开评测交叉验证。

---

## 一、先定性：我们能借鉴什么、不能借鉴什么

Realcast 当前能力（来自既有交付）：二元/多选市场、积分体系、Oracle 结算（ESPN + HTTP 源）、UGC + AI 分级审核、排行榜、锦标赛、实时广播、通知、徽章/连胜/等级、年龄门+密码安全。

**不能借鉴**（合规红线，硬性排除）：
- 任何真钱/加密货币结算（Polymarket、Kalshi、Augur、Omen、web3 系）→ 直接违反「积分只送不卖 / 去加密」。
- 这些只能作为**外部概率数据源**（只读喂价），绝不作为交易层。

**值得借鉴**的四个方向：
1. 市场机制数学（AMM / 限价单 / 共识概率）——来自 Manifold。
2. 评分与校准（proper scoring rules + 校准曲线 + 连续型问题）——来自 Metaculus。
3. 外部概率 Oracle 喂价（把 Polymarket/Kalshi 公开概率当参考源）——来自 pmxt / prediction-market-analysis。
4. 排名与声誉升级（不确定性感知排名 + 用预测准确度反哺声誉）——来自 propagon / TrustSwarm。

---

## 二、候选项目速览（已验证可访问）

| 项目 | 语言/栈 | License | Stars(约) | 核心可借鉴点 | 与 Realcast 契合度 |
|---|---|---|---|---|---|
| **Manifold** (manifoldmarkets/manifold) | TS monorepo / Firebase+Supabase | MIT | 万级 | 市场机制数学（LMSR/CPMM + 限价单）、市场类型（二元/多选/伪数值/投票/赏金）、季度联赛 | ★★★★★ 机制与定位最贴近 |
| **Metaculus** (openpredictionmarkets/metaculus) | Django+Postgres+Redis+React | BSD-2 | ~110 | 最佳评分/校准体系、连续分布问题、可靠性曲线 | ★★★★ 评分与连续问题最强 |
| **pmxt** (pmxt-dev/pmxt) | TS | — | ~2.1k | 统一 API 拉取 Polymarket/Kalshi 概率（"预测市场版 CCXT"） | ★★★★ 作外部 Oracle 喂价 |
| **prediction-market-analysis** (Jon-Becker) | Python | — | ~3.8k | 最大的公开 Polymarket/Kalshi 数据集，可基准测试 | ★★★ 训练/基准/种子市场 |
| **propagon** (Refefer/propagon) | Rust | — | 小众 | 贝叶斯 Bradley-Terry 排名 + 不确定性区间 | ★★★ 升级排行榜 |
| **TrustSwarm** | Node | — | 小众 | 声誉由预测准确度验证；Coral Protocol MCP 让 AI agent 发预测 | ★★★ 声誉+AI agent 预测 |
| **poly-maker** | Python | — | ~1.5k | 自动做市/双边挂单流动性机器人 | ★★ 流动性/初始概率播种 |
| **brierly / Anamnesis** | JS / Rust | — | 极小 | 轻量 Brier/log-loss 校准追踪器 | ★★ 校准功能最小实现参考 |

> 注：Manifold 全栈是 TS/Node，与我们的 Python 栈不一致——**只借鉴其市场机制「算法思想」**（common/ 里的 bet/sellBet 数学），不搬栈。Metaculus 同样栈不同，借鉴其评分/校准算法。pmxt/prediction-market-analysis/propagon 的思路可直接用 Python 复刻。

---

## 三、逐项借鉴研发方案

### 1. Manifold —— 市场机制数学（优先级 P1）
- **借鉴**：CPMM（常数乘积做市商）+ 限价单混合；市场类型扩展到「伪数值（范围内取值）」「赏金问题」。
- **为什么对我们有用**：当前 Realcast 是积分二元/多选，概率由用户下注共识驱动但缺一个清晰、可审计的做市曲线。借鉴 CPMM 可让「共识概率」有数学定义（如 `p = y/(x+y)`），结算/展示更稳，也便于做「初始概率播种」。
- **落地方式**：在 `app/core/markets.py` 新增纯函数 `cpmm_probability(shares_yes, shares_no)`、`cpmm_buy_cost(...)`（不引入真实资金，用积分模拟份额）。**保持积分只送不卖**——份额只是共识标量，不涉及余额转移。
- **风险**：Manifold 用 Firebase/Supabase 实时份额，我们是 SQLite+WAL，需把份额状态落库为普通表；并发写入走既有 thread-local 连接池即可。
- **合规**：只取算法，不取「买 Mana/提现」逻辑，红线安全。

### 2. Metaculus —— 评分与校准 + 连续型问题（优先级 P1）
- **借鉴**：proper scoring rules（对数分数 log-score、Brier 分数）、按问题类型的评分、用户级校准曲线（可靠性图）、连续/数值区间问题。
- **为什么对我们有用**：当前「准确度」只是二元对错计数，缺**概率校准**维度——一个总说 90% 却只对 60% 的用户，在合规预测社区里应有区分。校准曲线能让「靠谱用户」可量化，反哺声誉与等级。
- **落地方式**：
  - 新增 `app/core/scoring.py`：`log_score(p, outcome)`、`brier_score(p, outcome)`、连续型的 `log_score_continuous`。算法公开且简单（十几行），无需引第三方库。
  - 排行榜增加「校准分」维度；用户主页加可靠性曲线（前端复用现有 Chart 能力）。
  - 市场类型加「数值区间」（伪数值），Oracle 结算时取真实值算连续分数。
- **风险**：连续问题需要 Oracle 返回数值而非二元——我们已有 ESPN/http 源，扩展「数值型结算」即可。

### 3. pmxt / prediction-market-analysis —— 外部概率 Oracle 喂价（优先级 P2）
- **借鉴**：把 Polymarket/Kalshi 的**公开概率**作为「市场参考源」，只读。
- **为什么对我们有用**：当前 Oracle 主要是 ESPN（体育赛事结果）。对政治/经济/科技类题，外部公开共识概率可作「参考水位」，帮助：① 给新市场做初始概率播种；② 做「群体预测 vs 市场共识」的偏差分析（内容/研究价值）；③ 校验我们自有 Oracle 的合理性。
- **落地方式**：在 `app/core/oracle.py` 增加 `external_market_source`（HTTP 拉 pmxt 类端点或 prediction-market-analysis 数据集快照），**仅做参考展示与种子**，不参与结算（结算仍走我们自有 Oracle，合规隔离）。
- **合规**：只读外部公开概率、不交易、不嵌入任何加密/法币——红线安全。需要在 `合规就绪自评` 里补一条「外部喂价仅作参考，不具结算效力」。

### 4. propagon —— 不确定性感知排名（优先级 P2）
- **借鉴**：贝叶斯 Bradley-Terry / Glicko-2 排名，给每个用户一个**带置信区间的技能分**，而非单点。
- **为什么对我们有用**：当前排行榜是简单积分/连胜，新用户只下 2–3 注时排名极不稳定。引入不确定性区间后，可「隐藏低置信度用户的排名」或给种子概率，体验更公平，也与 Metaculus 的校准思路一致。
- **落地方式**：Python 复刻 Bradley-Terry/Glicko-2（算法公开，几十行）作为排行榜的「技能分」补充维度，原积分榜保留为公开榜。Rust 库本身不必引入（避免栈膨胀）。

### 5. TrustSwarm 思路 —— 声誉由预测准确度反哺 + AI agent 预测（优先级 P3）
- **借鉴**：① 声誉不靠主观好评，而靠「可验证的预测技能」累计；② 用 MCP/Coral 让 AI agent 直接发预测。
- **为什么对我们有用**：Realcast 已有 UGC 声誉门槛（新用户被拦），但声誉与「预测准不准」是两条线。把预测准确度纳入声誉，能与现有 AI 分级审核形成闭环。AI agent 发预测则可丰富「AI 协作预测」叙事（合规下 AI 仅作参与者，人工兜底）。
- **落地方式**：在 `app/core/points.py` 的声誉模型里增加「预测准确度」权重；探索用我们既有 automation/scout.py 框架让一个受控 AI agent 周期性发预测（走普通预测 API，受同样规则约束）。

### 6. poly-maker 思路 —— 流动性/初始概率播种机器人（优先级 P3）
- **借鉴**：自动双边挂单维持概率曲线。
- **落地方式**：复用我们 `app/automation/scout.py` 的定时任务框架，对新开市场用 CPMM 公式做「初始共识概率播种」，避免冷启动市场概率长期卡在 50%。

---

## 四、落地优先级路线图

| 优先级 | 项 | 工作量 | 依赖 | 合规影响 |
|---|---|---|---|---|
| **P1** | 市场机制数学（CPMM 共识概率） | 中 | markets.py 改造 | 安全（仅算法） |
| **P1** | 评分与校准 + 连续型问题 | 中 | scoring.py 新模块 | 安全（增强合规可信度） |
| **P2** | 外部概率 Oracle 喂价（只读参考） | 小 | oracle.py 扩展 | 需补合规声明 |
| **P2** | 不确定性感知排名 | 小 | 复刻 Bradley-Terry | 安全 |
| **P3** | 声誉×预测准确度 + AI agent 预测 | 中 | points/scout 改造 | 需人工兜底审计 |
| **P3** | 初始概率播种机器人 | 小 | scout 框架复用 | 安全 |

> 建议先做 P1 两项（机制数学 + 评分校准）：它们是「预测社区」区别于普通投票/竞猜的核心壁垒，且算法公开、纯 Python 可落地、不碰合规红线。

---

## 五、注意事项
- **License**：Manifold=MIT（可商用借鉴算法/代码思想，注意 MIT 要求保留版权声明，且我们只借鉴算法不搬其 Firebase 代码）；Metaculus=BSD-2（同样宽松）。借鉴时把源仓库与 license 注释写进我们对应模块头部，便于审计。
- **栈隔离**：上述项目多为 TS/Node，我们的栈是 Python/FastAPI/SQLite。**不引入对方运行时**，只移植算法（几行到几十行纯函数），避免依赖膨胀与合规面扩大。
- **合规铁律不变**：积分只送不卖 / 不可流通 / 不可回兑 / 去加密。任何「市场机制」「外部喂价」都只能在「共识标量」「参考展示」层，绝不允许余额转移或对接交易/加密。
- **Oracle 主权**：外部概率是「参考」，结算仍以我们自有 Oracle（ESPN/HTTP/人工）为准，避免被外部源带偏或操纵。

---

## 六、建议的下一步（确认后开干）
1. 先深读 Manifold `common/` 的 bet/sellBet 数学 + Metaculus 评分代码，产出「算法移植 spec」（纯函数签名 + 单测）。
2. 在 v0.7.8 起做 P1：新增 `scoring.py`，并把 markets.py 的概率计算改为 CPMM 共识公式（保留旧接口兼容）。
3. 同步在 `合规就绪自评与上线清单.md` 补「外部喂价仅参考」一条。

需要我直接开始 P1 的 `scoring.py`（log/Brier + 连续型）模块设计与实现吗？或先就某个项目做代码级深读？
